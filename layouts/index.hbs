<!DOCTYPE html>
<html>
  <head>
    {{> meta}}
    <title>{{sitename}} - {{page-title}}</title>
    <meta name="description" content="Beatriz Alonso - Personal Website">
    {{> styles}}
    <link rel="stylesheet" href="/styles/home.css" media="screen">
    {{> analytics}}
  </head>
  <body>
    {{> header}}
    <div id="wrapper">
      {{> menu}}
      <main id="page-home">
        <h2>Home</h2>
        <div id="project-grid-wrapper">
          {{#each projects}}
          {{! In case this goes nuts and `path` is rendered as [object Object]
          try updating node packages `npm update` }}
          <a href="{{path}}">
            <img src="/imgs/thumbs/{{thumb}}" alt="{{title}}" />
          </a>
          {{/each}}
        </div>
      </main>
    </div>
  <!--  <footer>
      <p>
        All the images are propety of Beatriz Alonso
      </p>
    </footer>-->
  </body>
</html>
